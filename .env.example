# Copy to .env for your own reference, or set these directly in your MCP client's
# config (claude_desktop_config.json / .claude/settings.json). The server itself
# reads these from process.env — it does not load .env files automatically.

RYBBIT_URL=https://app.rybbit.io
RYBBIT_API_KEY=your-api-key-here
